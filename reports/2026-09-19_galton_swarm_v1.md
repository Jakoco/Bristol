# 实验记录 — galton_swarm v1（合成数据首跑）

- 日期：2026-09-19
- 代码：commit `8f3f233`（`galton_swarm.py`，CFG 默认值，无改动）
- 运行环境：Colab **CPU**（已确认，3569s 属该硬件正常水平）
- 关联 tag：`exp-swarm-v1`

## 配置（具体值）

- 数据：`make_blobs` 合成高斯簇，F=8，K=14，N=2100，分层 60/20/20，split_seed=42
- 模型：P=256 粒子，T=24 排钉，G=33 钉列，M=16 上下文维度，mode='sde'，crowd_beta=1.0，参数 ≈13.6k
- 训练：NES，lr=0.02，σ=0.05（衰减 0.9995/代），pop=64（反对称对），600 代，batch=256，wd=1e-4，seed=42

## 结果

- **最佳 val acc = 0.9619**（14 类，随机 = 0.0714）
- 训练用时 **3569s**（600 代 × 64 个体）
- **测试集 acc = 0.9571**（单次前向离散度 ±0.0029）NLL 0.7939
- checkpoint 已存 Drive：`/content/drive/MyDrive/Checkpoints/galton_swarm.pt`
- 探针（取两个被正确分类的验证样本，类 0 与类 1）：
  - bean 轨迹呈高尔顿扇形：从 s=0 同口落入，逐排扩散，两样本终点云明显分离（样本0 收敛于 s≈−16 密集区，样本1 散布于 s≈−13）
  - 终点直方图：样本0 质量集中于类 0 格（≈0.81）；样本1 分布于类 1（≈0.47）/ 类 2（≈0.29）/ 类 0（≈0.20）——**读出=计数成立**，argmax 均正确
  - 证据流：两样本的分类差距主要在 **t≈15–24（末段钉排）**拉开；样本0 的类 0 证据在 t≈17 后急升，与类 1 证据出现可见的竞争——前 15 排是共享的混合段
  - 轨迹整体左偏：合法。学到的全局偏置，类 0 格本就在板左端

## 结论

- 三条设计准则机制层面全部跑通：零梯度（NES）训练收敛且精度显著高于随机；直方图读出有效；内部状态可读且确实携带分类信息
- 可读性首次实证：**决策位置（末段钉排）**是旧 MDU 结构无法回答的问题，现在直接可观测——这就是"模型的意义"的落点
- 单次前向离散度仅 ±0.0029：P=256 的群体在直方图层面自我平均，随机性几乎不引入推理方差——"统计输出"的稳定性得到实证
- 群智耦合（拥挤侧向力）与扩散极限 SDE 在训练后保留了非平凡的群体动力学（扇形扩散、末段分流）

## 已知问题 / 下轮改进

- 图标题中文乱码：Colab matplotlib 缺 CJK 字体，纯显示问题（下轮给图加英文标签或配置字体）
- 本轮跑在 **CPU**：3569s 属正常；下轮选 GPU runtime（T4）预计快一个量级；脚本未打印 device，下轮补一行；NES 逐个体评估如需再提速可并行化（暂不必）
- ρ 群密度热图本轮未截图核对，下轮随探针一并检查
- 下一步：换真实 Drive 数据（`F_data_X/Y`）；对比 `mode='bean'` 离散豆子模式
